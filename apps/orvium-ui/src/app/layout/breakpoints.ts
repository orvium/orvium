/**
 * Defines the media query breakpoints for responsive design in the application.
 * Currently includes a breakpoint for mobile devices.
 *
 * @property {string} MOBILE - Media query string for maximum width of 768 pixels, targeting mobile devices.
 */
export const BREAKPOINTS = {
  MOBILE: '(max-width: 768px)',
} as const;
